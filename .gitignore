__pycache__/
*.log
*.pid
.env
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.binlog
pipeline_settings.json

# Private/runtime data (not shipped)
saved_models.json
_app_v2
streamrecorder_crash.log

# Recording artifacts
*.ts
*.mp4
*.part

# Embedded-preview mpv library (large local binary; user-supplied, not shipped)
libmpv-2.dll
mpv-2.dll
mpv-1.dll
# Non-adult port — has its own repo, must never be tracked here
StreamingHub/
